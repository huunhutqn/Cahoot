{
  "name": "cahoot",
  "private": true,
  "scripts": {
    "dev": "echo 'Use: cd packages/web && npm run dev OR cd packages/socket && npm run dev'",
    "install:all": "cd packages/web && npm install && cd ../socket && npm install"
  }
}
