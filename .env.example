# Socket Server URL
# Copy to .env.local for custom configuration
# Next.js automatically loads:
# - .env.development (when running `npm run dev`)
# - .env.production (when running `npm run build` and `npm start`)

SOCKET_URL=http://localhost:3001
