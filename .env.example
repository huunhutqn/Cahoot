# Socket Server URL
# Set these environment variables on your server/Docker

# Server-side (Next.js server -> Socket Server)
SOCKET_URL=http://localhost:3001

# Client-side (Browser -> Socket Server)
NEXT_PUBLIC_SOCKET_URL=http://localhost:3001
